diff --git a/scripts/verification/probe_page_progress.js b/scripts/verification/probe_page_progress.js
index d6da9bc..ad559d2 100644
--- a/scripts/verification/probe_page_progress.js
+++ b/scripts/verification/probe_page_progress.js
@@ -25,2 +25,8 @@ const fs = require('node:fs');
   progress.reset();assert.equal(node.hidden,true);
+  progress.accept(run);
+  progress.accept({...run,attemptId:'fractional',startedAt:'2026-01-01T00:00:00.123Z',snapshotVersion:0,stage:'OCR',canRead:false});
+  assert.match(node.textContent,/10%/,'fractional ISO instant is newer than the same whole second');
+  progress.accept({...run,attemptId:'invalid',startedAt:'invalid',snapshotVersion:99});
+  assert.match(node.textContent,/10%/,'invalid attempt timestamp must not replace a known attempt');
+  progress.reset();
   const html=fs.readFileSync('src/main/resources/static/index.html','utf8');
diff --git a/src/main/java/studio/bookhtml/service/JobService.java b/src/main/java/studio/bookhtml/service/JobService.java
index 7e8ed30..5809362 100644
--- a/src/main/java/studio/bookhtml/service/JobService.java
+++ b/src/main/java/studio/bookhtml/service/JobService.java
@@ -40,3 +40,17 @@ public class JobService {
     @PostConstruct void recover(){store.recoverInterruptedJobs();reconcileAttemptIntents();}
-    @PreDestroy void close(){worker.shutdownNow();cancelAllReserved();}
+    @PreDestroy void close() {
+        synchronized (this) {
+            if (active != null) active.cancelled = true;
+            cancelAllReserved();
+            worker.shutdownNow();
+        }
+        // Never hold the admission lock while workers finish their commit/lease cleanup.
+        try {
+            if (!worker.awaitTermination(5, TimeUnit.SECONDS))
+                System.getLogger(JobService.class.getName()).log(System.Logger.Level.WARNING,
+                        "Page workers did not drain within the shutdown deadline");
+        } catch (InterruptedException interrupted) {
+            Thread.currentThread().interrupt();
+        }
+    }
     public synchronized void reserveReading(UUID reservation,String bookId){
@@ -524,3 +538,6 @@ public class JobService {
                             completeIntent(running.bookId, pageNumber, "BASELINE_PUBLISHED");
-                            if(attemptId!=null)progress.baselinePublished(running.bookId,pageNumber,attemptId,publishedRev,false);
+                            if(attemptId!=null) {
+                                progress.baselinePublished(running.bookId,pageNumber,attemptId,publishedRev,false);
+                                progress.unitDone(running.bookId,pageNumber,attemptId,true);
+                            }
                         } catch (PageConflictException | IOException conflict) {
diff --git a/src/main/java/studio/bookhtml/service/ProcessingProgressService.java b/src/main/java/studio/bookhtml/service/ProcessingProgressService.java
index d40425a..b56759e 100644
--- a/src/main/java/studio/bookhtml/service/ProcessingProgressService.java
+++ b/src/main/java/studio/bookhtml/service/ProcessingProgressService.java
@@ -115,3 +115,10 @@ public class ProcessingProgressService {
         if (e == null) return;
-        e.lifecycle = "SUCCEEDED".equals(lifecycle) && (e.incomplete || e.failed > 0 || e.skipped > 0 || e.cancelled > 0) ? "PARTIAL" : lifecycle; e.message = messageCode; e.canRetry = canRetry; e.inFlight = 0; e.changed();
+        boolean unfinished = e.incomplete || e.failed > 0 || e.skipped > 0 || e.cancelled > 0
+                || e.done() < e.total;
+        boolean partial = "SUCCEEDED".equals(lifecycle) && unfinished;
+        e.lifecycle = partial ? "PARTIAL" : lifecycle;
+        e.message = partial ? "WORK_PLAN_INCOMPLETE" : messageCode;
+        e.canRetry = canRetry || partial;
+        e.inFlight = 0;
+        e.changed();
     }
diff --git a/src/main/resources/static/page-progress.js b/src/main/resources/static/page-progress.js
index 19d0676..4714006 100644
--- a/src/main/resources/static/page-progress.js
+++ b/src/main/resources/static/page-progress.js
@@ -56,3 +56,6 @@ export function createPageProgress({ api, state, element }) {
     if (snapshot && snapshot.attemptId !== value.attemptId) {
-      if (String(value.startedAt || '') < String(snapshot.startedAt || '')) return;
+      const incomingStart = Date.parse(value.startedAt || '');
+      const currentStart = Date.parse(snapshot.startedAt || '');
+      // ISO instants can omit fractional seconds; lexical ordering is not chronological.
+      if (Number.isFinite(currentStart) && (!Number.isFinite(incomingStart) || incomingStart < currentStart)) return;
       retired.add(snapshot.attemptId);
diff --git a/src/test/java/studio/bookhtml/decision/StateBuilderTest.java b/src/test/java/studio/bookhtml/decision/StateBuilderTest.java
index aff3f17..50ae76b 100644
--- a/src/test/java/studio/bookhtml/decision/StateBuilderTest.java
+++ b/src/test/java/studio/bookhtml/decision/StateBuilderTest.java
@@ -48,3 +48,3 @@ class StateBuilderTest {
         assertFalse(gap.contains("best"));
-        assertEquals("question-template-v2", DecisionStateBuilder.TEMPLATE_VERSION);
+        assertEquals("question-template-v3-book-context", DecisionStateBuilder.TEMPLATE_VERSION);
     }
diff --git a/src/test/java/studio/bookhtml/service/ExportDecisionsTest.java b/src/test/java/studio/bookhtml/service/ExportDecisionsTest.java
index 6233fad..f4054fc 100644
--- a/src/test/java/studio/bookhtml/service/ExportDecisionsTest.java
+++ b/src/test/java/studio/bookhtml/service/ExportDecisionsTest.java
@@ -154,3 +154,3 @@ class ExportDecisionsTest {
         config.setCalibrationStatus("VALIDATED");
-        config.setCalibrationProfile("cal-v1|model=mock-model|template=question-template-v2"
+        config.setCalibrationProfile("cal-v1|model=mock-model|template=question-template-v3-book-context"
                 + "|candidate=candidate-config-v4|policy=decision-policy-v1"
@@ -208,3 +208,3 @@ class ExportDecisionsTest {
         assertNotNull(entry.get("decisionId"));
-        assertEquals("question-template-v2", entry.get("templateVersion"));
+        assertEquals("question-template-v3-book-context", entry.get("templateVersion"));
         assertEquals("decision-policy-v1", entry.get("policyVersion"));
diff --git a/src/test/java/studio/bookhtml/service/PageAttemptLifecycleTest.java b/src/test/java/studio/bookhtml/service/PageAttemptLifecycleTest.java
index 598353c..600021f 100644
--- a/src/test/java/studio/bookhtml/service/PageAttemptLifecycleTest.java
+++ b/src/test/java/studio/bookhtml/service/PageAttemptLifecycleTest.java
@@ -277,3 +277,3 @@ class PageAttemptLifecycleTest {
         settings.update(json.readTree("{\"revision\":0,\"ocr\":{\"ppocr\":{\"apiKey\":\"pp-key\",\"secretKey\":\"pp-secret\"}}}"));
-        // 中心页已就绪，tick 走后台填充，一次占满主通道 3 个槽位。
+        // 中心页已就绪：后台最多占 2 槽，预留 1 槽供跳页；次通道仍未获授权。
         store.writePage(book.id(), new Page(1, 600, 800, "READY", "paddle-aistudio",
@@ -282,3 +282,3 @@ class PageAttemptLifecycleTest {
         List<String> providers = new CopyOnWriteArrayList<>();
-        CountDownLatch entered = new CountDownLatch(3);
+        CountDownLatch entered = new CountDownLatch(2);
         CountDownLatch release = new CountDownLatch(1);
@@ -295,7 +295,9 @@ class PageAttemptLifecycleTest {
         windows.tick();
-        assertTrue(entered.await(3, TimeUnit.SECONDS));
-        assertTrue(providers.stream().allMatch("paddle-aistudio"::equals), "次通道请求数为 0");
-        release.countDown();
+        try {
+            assertTrue(entered.await(3, TimeUnit.SECONDS));
+            assertEquals(2, providers.size(), "后台不耗尽当前页保留槽");
+            assertTrue(providers.stream().allMatch("paddle-aistudio"::equals), "次通道请求数为 0");
+        } finally { release.countDown(); }
         // 等待后台写收尾再结束，避免临时目录清理时仍有打开句柄。
-        await(() -> "READY".equals(store.readPage(book.id(), 4).status()));
+        await(() -> "READY".equals(store.readPage(book.id(), 3).status()));
     }
diff --git a/src/test/java/studio/bookhtml/service/ProcessingProgressTest.java b/src/test/java/studio/bookhtml/service/ProcessingProgressTest.java
index ab06483..a173d35 100644
--- a/src/test/java/studio/bookhtml/service/ProcessingProgressTest.java
+++ b/src/test/java/studio/bookhtml/service/ProcessingProgressTest.java
@@ -101,5 +101,6 @@ class ProcessingProgressTest {
                 .thenAnswer(inv -> {
-                    enrichEntered.countDown();
-                    assertTrue(enrichRelease.await(4, TimeUnit.SECONDS));
                     int n = inv.getArgument(1);
+                    // Neighbours may enter enrichment before page 1. Observe this page only.
+                    if (n == 1) enrichEntered.countDown();
+                    assertTrue(enrichRelease.await(4, TimeUnit.SECONDS));
                     return new PageProcessor.EnrichResult(List.of(text("e" + n, "增强正文" + n)),
@@ -123,2 +124,3 @@ class ProcessingProgressTest {
         await(() -> "增强正文1".equals(store.readPage(book.id(), 1).blocks().get(0).original()));
+        await(() -> "SUCCEEDED".equals(progress.latest(book.id(), 1).lifecycle()));
         ProcessingSnapshot done = progress.latest(book.id(), 1);
diff --git a/src/test/java/studio/bookhtml/service/ReaderAuditRegressionTest.java b/src/test/java/studio/bookhtml/service/ReaderAuditRegressionTest.java
index bbd38f0..daa9844 100644
--- a/src/test/java/studio/bookhtml/service/ReaderAuditRegressionTest.java
+++ b/src/test/java/studio/bookhtml/service/ReaderAuditRegressionTest.java
@@ -29,2 +29,15 @@ class ReaderAuditRegressionTest {
     }
+    @Test void unfinishedWorkCannotBeReportedAsComplete() {
+        var service = new ProcessingProgressService();
+        UUID attempt = service.begin("book", 1, 0);
+        service.plan("book", 1, attempt, "REVIEW_CHUNK", 4);
+        service.unitDone("book", 1, attempt, true);
+        service.baselinePublished("book", 1, attempt, 2, false);
+        service.finish("book", 1, attempt, "SUCCEEDED", "ENHANCED", false);
+        var result = service.latest("book", 1);
+        assertEquals("PARTIAL", result.lifecycle());
+        assertEquals("WORK_PLAN_INCOMPLETE", result.messageCode());
+        assertTrue(result.canRead());
+        assertTrue(result.canRetry());
+    }
     @Test void concurrentUnitsAreAtomicClampedAndScopeSafe() throws Exception {
diff --git a/src/test/java/studio/bookhtml/service/UxU4ProgressTest.java b/src/test/java/studio/bookhtml/service/UxU4ProgressTest.java
index 61240ec..3e6b045 100644
--- a/src/test/java/studio/bookhtml/service/UxU4ProgressTest.java
+++ b/src/test/java/studio/bookhtml/service/UxU4ProgressTest.java
@@ -32,3 +32,4 @@ class UxU4ProgressTest {
         int commitIdx = jobs.indexOf("CommitOp.JOB_BASELINE");
-        int publishedIdx = jobs.indexOf("baselinePublished(running.bookId");
+        int reservedIdx = jobs.indexOf("private void runReserved(");
+        int publishedIdx = jobs.indexOf("baselinePublished(running.bookId", reservedIdx);
         assertTrue(commitIdx >= 0 && publishedIdx > commitIdx,
diff --git a/src/test/java/studio/bookhtml/service/UxU5QwenBoundedTest.java b/src/test/java/studio/bookhtml/service/UxU5QwenBoundedTest.java
index b6fe554..aa65693 100644
--- a/src/test/java/studio/bookhtml/service/UxU5QwenBoundedTest.java
+++ b/src/test/java/studio/bookhtml/service/UxU5QwenBoundedTest.java
@@ -49,3 +49,6 @@ class UxU5QwenBoundedTest {
         String config = read("src/main/java/studio/bookhtml/config/QwenAssistProperties.java");
-        assertTrue(config.contains("private boolean chunkedAssist = false"), "U5：分组默认关闭，旧整页为回滚");
+        assertTrue(config.contains("private boolean chunkedAssist = true"), "分组默认开启，仍需既有云外发授权");
+        assertTrue(config.contains("setChunkedAssist"), "操作员仍可明确关闭分组以回滚");
+        assertTrue(read("src/main/resources/application.properties").contains("QWEN_CHUNKED_ASSIST:true"),
+                "部署参数可覆盖默认值，不硬编码移除回滚入口");
         assertTrue(config.contains("maxConcurrentRequests = 3"), "U5：全局 3 为工程起点");
@@ -53,3 +56,3 @@ class UxU5QwenBoundedTest {
         String processor = read("src/main/java/studio/bookhtml/service/PageProcessor.java");
-        assertTrue(processor.contains("tryChunkedAssist"), "U5：分组优先、失败回滚整页");
+        assertTrue(processor.contains("tryChunkedAssist"), "分组优先；实际请求失败保留基线，不重复计费调用整页");
         assertTrue(processor.contains("qwenLayout.assist"), "U5：旧整页路径保留");
