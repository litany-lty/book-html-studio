# 首次启动可用此文件初始化；更推荐在网页“工具配置”填写。
# 界面设置保存在 BOOK_DATA_DIR/.settings/settings.json，并优先于环境变量。
# .env 和 settings.json 不应提交或分享。仅配置一条 OCR 即可使用。
PORT=18765
BOOK_DATA_DIR=./data
# 百度智能云应用：PP-OCRv6（官方请求路径仍为 pp_ocrv5）。
BAIDU_OCR_API_KEY=
BAIDU_OCR_SECRET_KEY=

# 飞桨 AI Studio：PaddleOCR-VL-1.6（默认通道，Access Token 独立认证）。
PADDLEOCR_ACCESS_TOKEN=
PADDLE_AISTUDIO_JOB_URL=https://paddleocr.aistudio-app.com/api/v2/ocr/jobs
PADDLE_AISTUDIO_MODEL=PaddleOCR-VL-1.6
PADDLE_AISTUDIO_REQUEST_TIMEOUT_SECONDS=60
PADDLE_AISTUDIO_TOTAL_TIMEOUT_SECONDS=180
PADDLE_AISTUDIO_POLL_INTERVAL_SECONDS=5

# 可选：阿里云 Qwen 结构整理与疑点建议。
# Key/模型/地域需一致；可在界面选择地域、Workspace ID，自动派生官方地址。
DASHSCOPE_API_KEY=
QWEN_ASSIST_ENABLED=false
QWEN_ASSIST_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_ASSIST_MODEL=qwen3.8-max

# JEV 默认关闭；需要时在界面设置密钥、模型、外发授权和调用核算上限。
# 本书费用参考单价也在界面填写，不配置时仍记录请求，用量未知不记成 0 元。

# 本机 Maven 默认 JDK 与 java 可能不同，必要时指定 JDK 17 或以上。
# JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-17.jdk/Contents/Home
